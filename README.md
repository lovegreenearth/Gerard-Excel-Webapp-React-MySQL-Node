# Gerard-Excel-Webapp-React-MySQL-Node

### Install DB
`cd backend`
- install excel-price.sql to Maria DB

### Backend
`cd backend`
`npm install`
`npm run dev`

### Frontend
`cd frontend`
`npm install`
`npm run start`
